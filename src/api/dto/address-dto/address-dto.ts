export interface ProvinceDto {
    code: string;
    name: string;
  }
  
  export interface DistrictDto {
    code: string;
    name: string;
  }
  
  export interface WardDto {
    code: string;
    name: string;
  }
  